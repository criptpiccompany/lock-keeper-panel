services:
  web:
    build:
      context: .
    container_name: criptpic-board-web
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    restart: unless-stopped

volumes:
  node_modules:
