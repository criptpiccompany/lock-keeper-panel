services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    env_file:
      - .env
    volumes:
      - .:/app
      - app_node_modules:/app/node_modules
    restart: unless-stopped

volumes:
  app_node_modules:
